/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['latelkcollege.liveblog365.com', 'images.pexels.com', 'pixabay.com'],
  },
};

module.exports = nextConfig;
